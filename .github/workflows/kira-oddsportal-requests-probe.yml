name: KIRA OddsPortal Requests Probe
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/kira_oddsportal_requests_probe.py'
      - '.github/workflows/kira-oddsportal-requests-probe.yml'
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check requests
      - run: python tools/kira_oddsportal_requests_probe.py
