name: KIRA Juancito NFL Exact Inventory
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/kira_juancito_nfl_exact_inventory_probe.py'
      - '.github/workflows/kira-juancito-nfl-exact-inventory.yml'
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python -m pip install --disable-pip-version-check playwright
          python -m playwright install --with-deps chromium
      - run: python tools/kira_juancito_nfl_exact_inventory_probe.py
      - uses: actions/upload-artifact@v4
        with:
          name: kira-juancito-nfl-exact-inventory
          path: artifacts/kira_juancito_nfl_exact_inventory
          if-no-files-found: error
          retention-days: 7
