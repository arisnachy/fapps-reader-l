name: KIRA Juancito Football MULTI3 Live
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/kira_juancito_football_multi3_live_probe.py'
      - '.github/workflows/kira-juancito-football-multi3-live.yml'
permissions:
  contents: read
jobs:
  football_multi3_live:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Playwright
        run: |
          python -m pip install --disable-pip-version-check playwright
          python -m playwright install --with-deps chromium
      - name: Run current read-only MULTI3 selector and exact +1.5 lookup
        run: python tools/kira_juancito_football_multi3_live_probe.py
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: kira-juancito-football-multi3-live
          path: artifacts/kira_juancito_football_multi3_live
          if-no-files-found: error
          retention-days: 7
